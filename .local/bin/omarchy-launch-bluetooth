#!/bin/bash
# Launch/focus bluetui TUI (single instance)

sudo rfkill unblock bluetooth 2>/dev/null

if swaymsg -t get_tree 2>/dev/null | grep -q '"app_id":"TUI.float"'; then
    swaymsg '[app_id="TUI.float"]' focus
else
    kitty --class=TUI.float -e bluetui
fi
