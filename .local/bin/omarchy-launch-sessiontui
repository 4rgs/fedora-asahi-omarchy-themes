#!/bin/bash
# Launch/focus session TUI (single instance)

if swaymsg -t get_tree 2>/dev/null | grep -q '"app_id":"TUI.float"'; then
    swaymsg '[app_id="TUI.float"]' focus
else
    kitty --class=TUI.float -e /home/4rgs/.local/bin/omarchy-sessiontui
fi
